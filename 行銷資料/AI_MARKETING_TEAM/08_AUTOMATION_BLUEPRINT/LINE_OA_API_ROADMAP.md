# LINE_OA_API_ROADMAP

Phase 5 is planning only.

## Future Flow
Approved LINE copy -> CSV/Google Sheets -> human check -> optional Messaging API push after separate approval.

## Required Future Work
Channel permission review, message quota review, opt-in compliance, unsubscribe handling, test channel dry run and credential vault.

## Forbidden Now
No channel access token, no channel secret, no push, no broadcast.
